#!groovy
node
{
    
    def json = readJSON file: "${env.WORKSPACE}\\servers.json"
    
    stage("Prepearing workspace")
    {
    json['servers'].each{ server ->
            echo '${server}'
        }
    }
}
