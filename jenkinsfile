pipeline {
    agent any
    severs = readJSON file: "${env.WORKSPACE}\\servers.json"
    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
